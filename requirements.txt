# Data Masking System 3.x — залежності для запуску з сирців
# (еквівалент `pip install '.[full]'`). Python 3.9+
#
# Ядро:
faker>=20.0.0

# Опційні (код деградує коректно без них):
# — шифрування mapping (AES-256-GCM):
cryptography>=41.0.0
# — YAML-конфігурація:
pyyaml>=6.0

# Тестові залежності — у requirements-dev.txt
